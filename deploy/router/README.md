[router.yaml](router.yaml) - файл который указывает на каком порту `router` 
будет слушать входящие graphql запросы

[schema.yaml](supergraph-config.yaml) - как router должен получать схему и 
динамически генерировать ее