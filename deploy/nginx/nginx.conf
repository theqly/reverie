events {}

http {
  server {
    listen 80;
    server_name xyi.local;
    location / {
      root /usr/share/nginx/html;  # Указываем путь к директории с HTML файлами
      index index.html;
    }
  }
}
