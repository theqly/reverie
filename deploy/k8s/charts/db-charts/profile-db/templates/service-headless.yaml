apiVersion: v1
kind: Service
metadata:
  name: {{ include "profile-db.fullname" . }}-headless
  labels:
    {{- include "profile-db.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    {{- include "profile-db.selectorLabels" . | nindent 4 }}
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
