apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "profile-db.fullname" . }}-test-connection"
  labels:
    {{- include "profile-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "profile-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
