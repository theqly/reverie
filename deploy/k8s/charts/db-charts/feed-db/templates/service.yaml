apiVersion: v1
kind: Service
metadata:
  name: {{ include "feed-db.fullname" . }}
  labels:
    {{- include "feed-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: postgres
      port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
  selector:
    {{- include "feed-db.selectorLabels" . | nindent 4 }}