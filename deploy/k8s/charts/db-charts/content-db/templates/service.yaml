apiVersion: v1
kind: Service
metadata:
  name: {{ include "content-db.fullname" . }}
  labels:
    {{- include "content-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: postgres
      port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
  selector:
    {{- include "content-db.selectorLabels" . | nindent 4 }}